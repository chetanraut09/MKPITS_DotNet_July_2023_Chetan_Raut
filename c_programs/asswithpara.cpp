#include<stdio.h>
int area( int n1,  int n2)
{
	int side= n1*n2;
	return side;
	{
		int main()
		do
		int n1,n2,side;
		printf("enter 2 no");
		scanf("%d%d=%d",&n1,&n2);
		side=n1*n2;
		printf("result=%d",side);
		fflush(stdin);
		scanf("%d",side);
	}
	while(side==side);
}
